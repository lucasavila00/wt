# Client config samples

Copy a sample to `~/.wt/config.toml`:

```bash
mkdir -p ~/.wt
cp client-config/wt.development.toml ~/.wt/config.toml
```

Each context names a local or OpenSSH WT server. See
[CLI and SSH](../docs/arch/cli.md#contexts).
