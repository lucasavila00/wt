# Client config

Copy the sample to `~/.wt/config.toml`:

```bash
mkdir -p ~/.wt
cp examples/client-config/wt.development.toml ~/.wt/config.toml
```

Each context names a local or OpenSSH WT server. See
[Client transport](../../docs/how/cli.md#context-transport).
