Not addressed by the Codex session metadata work: define a file-backed editable
world/task specification and its YAML lifecycle before implementing it.

--

# tasks

- created on the server

- once the server has enough resources, run the task

- repo + cd (or shell)

- then codex invocation

- once codex 'request attention' or finishes, we take a 'screenshot' of the byobu shell, stop the world and save it as a task ready.