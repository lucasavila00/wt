deleting a worlds should use the same cancelalble toast progress ui as creation.

the deletion uses a different progress than creation that uses the toast

ask sub agents to review the ui and make sure we are using consistent design system. both the impl and the look and feel.

go to the extra mile of using reusable abstractions to make sure it is the same ux on the whole app