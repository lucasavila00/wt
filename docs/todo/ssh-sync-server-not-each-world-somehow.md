maybe we should have a virtio shared folder for the authorized keys?

Not addressed by the Codex session metadata work: design host-level SSH
inventory synchronization without requiring every world to participate.
