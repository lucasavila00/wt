# wt-workload-registry

SQLite world registry and host-capacity admission.

It owns migrations, world and disk records, latest per-world Byobu pane
observations, and atomic CPU, RAM, and disk reservations.
