# wt-provider

Backend-neutral machine and guest-transport contracts.

It defines machine identity and resources, NoCloud input, lifecycle operations,
bounded guest commands, and transport errors. It contains no devcontainer,
host, GitHub, or libvirt policy.

Boundaries: [Provider boundaries](../../docs/internals/provider.md).
