[package]
name = "wt-api"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Shared JSON types for wt CLI and agents"

[dependencies]
# serde / serde_json — add when types land
